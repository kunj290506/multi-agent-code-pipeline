version: "3.9"

# ─────────────────────────────────────────────
#  Multi-Agent Code Pipeline — Infrastructure
#  Services: n8n · Ollama · ChromaDB (Local)
# ─────────────────────────────────────────────

services:

  # ── n8n: Visual workflow orchestration ──────
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=changeme
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://localhost:5678/
      - GENERIC_TIMEZONE=Asia/Kolkata
    volumes:
      - n8n_data:/home/node/.n8n
      - ./n8n:/home/node/.n8n/workflows   # persist exported workflow JSONs
    extra_hosts:
      - "host.docker.internal:host-gateway"   # Linux compatibility
    depends_on:
      ollama:
        condition: service_healthy
    networks:
      - pipeline-net

  # ── Ollama: Local LLM inference (Disabled - running on host) ─────────────
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    environment:
      - OLLAMA_HOST=0.0.0.0
    volumes:
      - ollama_data:/root/.ollama          # persist downloaded models
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:11434/api/tags || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - pipeline-net



  # ── Agent Services (commented out — Ollama runs on the host, not in a container,
  #    because GPU passthrough to a containerised Ollama requires WSL2 + CUDA-on-WSL.
  #    On a 4 GB GPU that is a hardware risk. Uncomment these blocks on capable hardware.)
  # ─────────────────────────────────────────────────────────────────────────────────────

  # planner-agent:
  #   build:
  #     context: ./agents/planner
  #   container_name: planner-agent
  #   restart: unless-stopped
  #   ports:
  #     - "8010:8010"
  #   environment:
  #     - OLLAMA_BASE_URL=http://host.docker.internal:11434
  #     - AGENT_PORT=8010
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"
  #   networks:
  #     - pipeline-net

  # rag-agent:
  #   build:
  #     context: ./agents/rag
  #   container_name: rag-agent
  #   restart: unless-stopped
  #   ports:
  #     - "8011:8011"
  #   environment:
  #     - OLLAMA_BASE_URL=http://host.docker.internal:11434
  #     - AGENT_PORT=8011
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"
  #   networks:
  #     - pipeline-net

  # codegen-agent:
  #   build:
  #     context: ./agents/codegen
  #   container_name: codegen-agent
  #   restart: unless-stopped
  #   ports:
  #     - "8014:8014"
  #   environment:
  #     - OLLAMA_BASE_URL=http://host.docker.internal:11434
  #     - AGENT_PORT=8014
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"
  #   networks:
  #     - pipeline-net

  # reviewer-agent:
  #   build:
  #     context: ./agents/reviewer
  #   container_name: reviewer-agent
  #   restart: unless-stopped
  #   ports:
  #     - "8015:8015"
  #   environment:
  #     - OLLAMA_BASE_URL=http://host.docker.internal:11434
  #     - AGENT_PORT=8015
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"
  #   networks:
  #     - pipeline-net

  # db-agent:
  #   build:
  #     context: ./agents/db
  #   container_name: db-agent
  #   restart: unless-stopped
  #   ports:
  #     - "8012:8012"
  #   environment:
  #     - OLLAMA_BASE_URL=http://host.docker.internal:11434
  #     - AGENT_PORT=8012
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"
  #   networks:
  #     - pipeline-net

  # webapp-backend:
  #   build:
  #     context: ./webapp/backend
  #   container_name: webapp-backend
  #   restart: unless-stopped
  #   ports:
  #     - "8020:8020"
  #   environment:
  #     - OLLAMA_BASE_URL=http://host.docker.internal:11434
  #     - AGENT_PORT=8020
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"
  #   networks:
  #     - pipeline-net


# ─────────────────────────────────────────────
#  Named Volumes
# ─────────────────────────────────────────────
volumes:
  n8n_data:
  ollama_data:

# ─────────────────────────────────────────────
#  Networks
# ─────────────────────────────────────────────
networks:
  pipeline-net:
    driver: bridge
